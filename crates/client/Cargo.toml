[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { version = "0.1.0", path = "../common", features = ["client"] }
gpui = "0.2.2"
gpui-component = "0.5.1"
gpui-component-assets = "0.5.1"
tokio = {version = "1.50.0", features = ["full"]}
